version: '3'
services:
  vue:
    container_name: front-web
    image: nginx
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - "8080:80"

